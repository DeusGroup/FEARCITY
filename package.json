{
  "name": "fear-city-cycles-fresh",
  "version": "1.0.0",
  "description": "Fear City Cycles - Fresh Vercel Deployment",
  "scripts": {
    "build": "mkdir -p public && cp -r assets bikes gear cart contact culture.html index.html main.html about.html offline.html robots.txt sitemap.xml sw.js public/",
    "postinstall": "echo 'Static deployment'"
  },
  "dependencies": {
    "@prisma/client": "^5.1.1",
    "prisma": "^5.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}